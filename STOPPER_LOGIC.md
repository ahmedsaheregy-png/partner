# 📖 توثيق منطق الستوبر (Stopper Logic)

## المفهوم الأساسي: العمق الثابت (Fixed Depth)

### ✅ الستوبر = عمق ثابت لكل الأعضاء

كل عضو في الشجرة، **بغض النظر عن موقعه**، يحصل على عمولات من:
- **نفسه** (المستوى 0)
- **+ 10 أجيال تحته** (المستويات 1-10)
- **= 11 مستوى إجمالاً**

---

## المعادلة المستخدمة

```javascript
let maxDepth = gens - 1;  // عندما gens = 11، يكون maxDepth = 10
```

### لماذا `gens - 1` وليس `gens`؟

الدالة `getTeamSizeWithLimit(maxDepth)` تعمل هكذا:
- `maxDepth = 0` ← يحسب العضو **نفسه فقط** = 1 مستوى
- `maxDepth = 1` ← يحسب العضو **+ 1 تحته** = 2 مستوى
- `maxDepth = 10` ← يحسب العضو **+ 10 تحته** = 11 مستوى ✅

لذلك للحصول على 11 مستوى، نمرر `10` (أي `11 - 1`).

---

## أمثلة توضيحية

### بفرض عدد الأجيال = 11:

| موقع العضو في الشجرة | maxDepth | عدد المستويات التي يأخذ منها عمولة |
|----------------------|----------|-----------------------------------|
| الشخص الأول (جيل 1) | 10 | 11 مستوى ✅ |
| عضو في الجيل 5 | 10 | 11 مستوى ✅ |
| عضو في الجيل 10 | 10 | 11 مستوى ✅ |
| عضو في الجيل 20 | 10 | 11 مستوى ✅ |

**الجميع يحصلون على نفس الحق!**

---

## ❌ ما هو العمق النسبي (الخاطئ)؟

كان هناك منطق خاطئ سابقاً يحسب:
```javascript
let maxDepth = gens - member.generation;  // ❌ خطأ
```

هذا كان يجعل:
- عضو جيل 1: يأخذ 10 مستويات تحته ✅
- عضو جيل 5: يأخذ 6 مستويات تحته فقط ❌
- عضو جيل 10: يأخذ مستوى واحد تحته فقط ❌

**هذا غير عادل وتم تصحيحه!**

---

## أين يُستخدم هذا المنطق؟

1. **حساب الجدول** (`traverseComm` في `renderTableAndCalc`)
2. **بطاقة معلومات العضو** (`showMemberInfo`)
3. **عرض Top 100** (`showTop100`)

جميعها تستخدم نفس المعادلة: `maxDepth = gens - 1`

---

## المصطلحات الهامة

| المصطلح | التعريف |
|---------|---------|
| **الستوبر** | حد الأجيال التي يحصل عليها العضو عمولة (11 جيل افتراضياً) |
| **فريق العمولات** | الأعضاء داخل نطاق الستوبر (الـ 11 مستوى) |
| **الفريق الرديف** | الأعضاء خارج نطاق الستوبر (بعد الـ 11 مستوى) |
| **العمق الثابت** | كل عضو يحصل على نفس عدد المستويات بغض النظر عن موقعه |

---

## ملاحظة للمطورين

⚠️ **تحذير:** لا تستخدم أبداً `gens - member.generation` لحساب الستوبر.
✅ **استخدم دائماً:** `gens - 1` للحصول على عمق ثابت لجميع الأعضاء.

---

*آخر تحديث: 2025-12-17*
